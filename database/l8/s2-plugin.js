// jQueryPlugin
(function($) {
    
    $.fn.levelApp42 = function(action, options) {

        var settings = $.extend({
            color: "inherit",
            backgroundColor: "inherit"
        }, options);


        if (action === "start") {

        }
        
        if (action === "stop") {
            
        }

        return this;
    };
    
}(jQuery));