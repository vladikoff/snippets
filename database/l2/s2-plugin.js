// jQueryPlugin
(function($) {
    
    $.fn.levelApp82 = function(action, options) {

        var settings = $.extend({
            color: "inherit",
            backgroundColor: "inherit"
        }, options);


        if (action === "start") {

        }
        
        if (action === "stop") {
            
        }

        return this;
    };
    
}(jQuery));